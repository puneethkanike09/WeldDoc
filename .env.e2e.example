# Copy to .env.e2e.local and fill in real credentials (file is gitignored).
E2E_USER_EMAIL=engineer@example.com
E2E_USER_PASSWORD=your-password
PLAYWRIGHT_BASE_URL=http://localhost:3000
# Set to 1 when the dev server is already running on PLAYWRIGHT_BASE_URL
# PLAYWRIGHT_SKIP_WEBSERVER=1
